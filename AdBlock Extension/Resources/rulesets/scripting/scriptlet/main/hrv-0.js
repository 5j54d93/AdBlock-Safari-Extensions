/*******************************************************************************

    AdBlock

    Generated scriptlet data for compiled filter-list rules.

*/

// ruleset: hrv-0
// world: MAIN

(function adblockGeneratedScriptlets() {

if ( self.__adblockSkipGoogleSearch === true ) { return; }

'use strict';

self.adblockRunScriptlets?.({
    rulesetId: "hrv-0",
    world: "MAIN",
    functions: ["setConstant"],
    args: ["googletag.destroySlots","noopFunc"],
    arglists: "0,0,1",
    arglistRefs: "0",
    hostnames: ["olx.ba"],
    regexes: [],
});

})();

void 0;
